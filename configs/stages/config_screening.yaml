# Stage skeleton — frozen / already-designed keys only.
# Stage name source of truth: configs/pipeline.yaml.

stage: config_screening
scope: validation_only
holdout_test_contact: false

inputs:
  raw_data_manifest: data/manifests/config_screening_inputs.yaml   # placeholder; file not created in Phase 6

outputs:
  results_dir: results/config_screening/
  run_manifest: artifacts/run_manifests/config_screening/          # gitignored at runtime
