# Stage skeleton — frozen / already-designed keys only.
# Stage name source of truth: configs/pipeline.yaml.

stage: controlled_followup
scope: validation_only
holdout_test_contact: false

inputs:
  raw_data_manifest: data/manifests/controlled_followup_inputs.yaml   # placeholder; file not created in Phase 6

outputs:
  results_dir: results/controlled_followup/
  run_manifest: artifacts/run_manifests/controlled_followup/          # gitignored at runtime
