# Stage skeleton — frozen / already-designed keys only.
# Stage name source of truth: configs/pipeline.yaml.

stage: model_family_screening
scope: validation_only
holdout_test_contact: false

inputs:
  raw_data_manifest: data/manifests/model_family_screening_inputs.yaml   # placeholder; file not created in Phase 6

outputs:
  results_dir: results/model_family_screening/
  run_manifest: artifacts/run_manifests/model_family_screening/          # gitignored at runtime
